
Define Node
  // attributes
  data
  left
  right
  height

  // constructor
  Function Node(data, height)
    data = data
    height = height
    left = null
    right = null

Define AVLTree
  // attributes
  root

  Function AVLTree(data)
    root = new Node(data, 0)

  Function insert(node, data)
